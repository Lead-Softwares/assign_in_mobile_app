import 'package:flutter/material.dart';

class CompanyAssets extends StatelessWidget {
  const CompanyAssets({super.key});
  static const routeName = '/company-assets';

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
